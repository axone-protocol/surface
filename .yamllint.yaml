extends: default
ignore: |
  dist/
  node_modules/
  .direnv/
  pnpm-lock.yaml

rules:
  document-start: disable
  line-length:
    max: 160
    level: warning
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 1
